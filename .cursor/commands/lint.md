# lint

Execute`poetry run pytest` to run the tests and fix all the issues until there are no more issues.
Execute `poetry run pre-commit run --all-files` to lint the code and fix all the issues until there are no more issues.
